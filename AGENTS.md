# UI 开发约束

任何涉及页面、组件、样式、交互动画或新产品功能 UI 的修改，都必须先阅读：

- `docs/DESIGN_SYSTEM.md`

实现要求：

1. 优先复用现有组件和 CSS 语义类。
2. 不得新增第二套字体、数据表圆角、状态动画或 CTA 样式。
3. 新功能完成后必须按照设计系统中的 UI 验收清单检查。
4. 必须在浏览器检查受影响页面并运行项目测试。
5. 若功能需求必须违反设计规范，应先记录原因并获得明确确认。
